{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "prepare": "husky",
    "precommit": "lint-staged"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.0.14",
    "nuxt": "^3.13.0",
    "pts": "^0.12.9",
    "tailwindcss": "^4.0.14",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.11"
  },
  "prettier": {
    "plugins": [
      "prettier-plugin-tailwindcss"
    ]
  },
  "lint-staged": {
    "*.{vue,ts,json,md}": "prettier --write"
  }
}
